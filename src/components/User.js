const User = ({ name, location }) => {
  return (
    <div className="userCard">
      <h2>Name: {name}</h2>
      <h3>Location: {location}</h3>
      <h4>User Name: coderam95</h4>
    </div>
  );
};

export default User;
